#include <iostream>

#include <ctime>
#include <vector>

#include "reader.h"
#include "point.h"
#include "triangle.h"
#include "camera.h"
#include "image.h"

using namespace std;

int main(){
    // vector <Triangle> triangles = create_triangles("samples/airboat.obj");

    // vector <vector<Pixel>> pixels;
    //
    // for (size_t i = 0; i < 87; i++) {
    //     vector <Pixel> pixels_1;
    //     for (size_t j = 0; j < 51; j++) {
    //         int r = rand()%255;
    //         int g = rand()%255;
    //         int b = rand()%255;
    //         Pixel pixel(r, g, b);
    //         pixels_1.push_back(pixel);
    //     }
    //     pixels.push_back(pixels_1);
    // }
    // Image image(87, 51, pixels);
    // image.write("test.bmp");
}
